package com.immomo.push.demo;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

//import com.cosmos.photon.push.channel.ChannelConstant;

/**
 * Created by wangduanqing on 2018/11/14.
 */

public class PushClickReceiver extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent extraIntent) {
//        MoNotify notify = (MoNotify) extraIntent.getSerializableExtra(ChannelConstant.Keys.KEY_PUSH_DATA);
//
//        PhotonPushManager.getInstance().onPushClick(notify);
//        Intent intent = new Intent(context, SecondActivity.class);
//        intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
//        context.startActivity(intent);
    }
}
